import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  empty: {},
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});